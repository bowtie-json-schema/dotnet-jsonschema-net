# dotnet-jsonschema-net

A [Bowtie](https://github.com/bowtie-json-schema/bowtie) test harness.

Its image is published to `ghcr.io/bowtie-json-schema/dotnet-jsonschema-net` and run via `bowtie run -i dotnet-jsonschema-net`.
